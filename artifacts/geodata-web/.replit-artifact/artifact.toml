kind = "web"
previewPath = "/"
title = "GeoDataPlacefinder"
version = "1.0.0"
id = "artifacts/geodata-web"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25276

[services.development]
run = "pnpm --filter @workspace/geodata-web run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/geodata-web", "run", "build" ]
publicDir = "artifacts/geodata-web/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25276"
BASE_PATH = "/"
